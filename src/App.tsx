import { useState } from 'react'

import web3Onboard from './onboard'

function App() {
  return (
    <div className="bg-grey-100 flex flex-col justify-center items-center min-h-screen relative">
      <button>Connect Wallet</button>
    </div>
  )
}

export default App
